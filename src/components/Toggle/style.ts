import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderWidth: 4,
    width: 150,
    height: 50,
    borderRadius: 50,
    overflow: 'hidden',
  },
  background: {
    width: '100%',
    height: '100%',
    justifyContent: 'center',
    paddingLeft: 5,
  },
  image: {
    width: 40,
    height: 40,
  },
});
